/**
 * Spring Data JPA repository interfaces: extend {@code JpaRepository} or {@code CrudRepository}.
 * Only used by adapters in {@code persistence.adapter} — never called from application layer directly.
 */
package br.com.contractguard.infrastructure.persistence.repository;
