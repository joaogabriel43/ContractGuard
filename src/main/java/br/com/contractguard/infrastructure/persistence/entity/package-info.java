/**
 * JPA entity classes: database-mapped objects. These NEVER enter the domain layer.
 * Mapping between JPA entities and domain objects is done in {@code persistence.adapter}.
 */
package br.com.contractguard.infrastructure.persistence.entity;
