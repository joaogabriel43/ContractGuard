/**
 * Use Case implementations: orchestrate domain objects to fulfill application use cases.
 * Each class implements exactly one Input Port interface.
 */
package br.com.contractguard.application.usecase;
